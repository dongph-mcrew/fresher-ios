import Cocoa

/*:
 Functions in swift is also a type. Which means you can store them in a variable or a constants or even pass them to other function whose parameter is a closure.
 Note: To use a closure you must use parentheses.
 */
let driving = {
    print("I'm driving in my car")
}

driving()
