//: [Previous](@previous)

import Foundation

//Functions with parameter

func square(number: Int) {
    print(number * number)
}

square(number: 8)
/*:
 Note: If a function has too many parameter then there is 3 ways to reduce it:
    -Does it need all the parameter?
    -Split up that function into smaller function to take less parameter
    -Should those parameters be grouped somehow
 */
//: [Next](@next)
